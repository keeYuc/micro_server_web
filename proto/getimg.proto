syntax = "proto3";

package getimg;

option go_package = "/;message";

service Getimg {
	rpc Call(Message) returns (Message) {}
	rpc Test(M_test) returns(M_test){}
}

message Message{
	string uuid = 1; //传入
	bytes  data = 2; //传出
}

message M_test{
	string test = 1;
}